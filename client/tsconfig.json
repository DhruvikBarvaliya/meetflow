{
  "$schema": "https://json.schemastore.org/tsconfig",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "moduleDetection": "force",
    "jsx": "react-jsx",
    "useDefineForClassFields": true,

    /* Strictness — mirrors tsconfig.base.json; the client is held to the same bar. */
    "strict": true,
    "noImplicitOverride": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedIndexedAccess": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "useUnknownInCatchVariables": true,
    "forceConsistentCasingInFileNames": true,
    "verbatimModuleSyntax": true,
    "isolatedModules": true,

    /* Vite owns the emit; tsc only ever type-checks. */
    "noEmit": true,
    "allowImportingTsExtensions": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "resolveJsonModule": true,
    "skipLibCheck": true,
    "pretty": true,

    /* No `baseUrl`: since TypeScript 4.4 a `paths` entry that is already
       relative resolves against this file's directory, so declaring one adds a
       second way to write a module specifier and nothing else. Vite resolves
       the same alias independently — see the `resolve.alias` in vite.config.ts,
       which is what actually rewrites these imports at build time. */
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src"]
}
