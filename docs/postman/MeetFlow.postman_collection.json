{
  "info": {
    "_postman_id": "d01f3f10-a8cc-4dd0-8ccb-e429d4c635b3",
    "name": "MeetFlow API v1.0.0",
    "description": "Generated from the MeetFlow OpenAPI document, which is itself generated from the zod\nschemas the server validates with. Do not edit by hand — run\n`npm --workspace server run contracts:export` instead.\n\n## Getting started\n\n1. Set `baseUrl` if the API is not on http://localhost:4000.\n2. Run **Auth › Sign in**. Its test script captures `accessToken` into a collection\n   variable, and every authenticated request picks it up from the collection-level\n   bearer auth.\n3. Run **Auth › Read the authenticated user** once. Its test script captures\n   `businessId`, which is sent as the `X-Business-Id` header. That header is optional\n   when the account belongs to exactly one workspace and required when it belongs to\n   several, so it is enabled on every workspace-scoped request.\n\nRequests under **Public Booking** are unauthenticated by design and are set to\n`noauth` so the bearer token is not sent to them.\n\n## Reading the examples\n\nOptional query parameters and optional headers ship disabled: each request runs as\nsent, and every filter the endpoint supports is one checkbox away.\n\nExample bodies name **every** field an endpoint accepts, so nothing is hidden — which\nmeans they also name mutually exclusive ones. A booking link, for instance, lists\n`serviceId`, `teamId` and `staffProfileId`, but its `type` decides which single one is\nlegal and naming the others is a 422. Trim each body to the combination you want.\n\nValues are placeholders, not fixtures: ids are all-zero UUIDs and free text is a stand-in.\nReplace them with ids from your own workspace.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "auth": {
    "type": "bearer",
    "bearer": [
      {
        "key": "token",
        "value": "{{accessToken}}",
        "type": "string"
      }
    ]
  },
  "variable": [
    {
      "key": "baseUrl",
      "value": "http://localhost:4000",
      "type": "string",
      "description": "The API origin. Request paths already carry the /api/v1 prefix."
    },
    {
      "key": "accessToken",
      "value": "",
      "type": "string",
      "description": "Captured by the Sign in request. Short-lived; sign in again when it expires."
    },
    {
      "key": "refreshToken",
      "value": "",
      "type": "string",
      "description": "Captured by the Sign in request. Used by Refresh and Sign out."
    },
    {
      "key": "businessId",
      "value": "",
      "type": "string",
      "description": "Sent as X-Business-Id to choose which workspace a request acts in. Captured by the \"Read the authenticated user\" request."
    }
  ],
  "item": [
    {
      "name": "Health",
      "description": "Liveness, readiness and build metadata. Mounted outside /api/v1 so probes never sit behind versioning or authentication.",
      "item": [
        {
          "name": "Liveness probe",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/health",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "health"
              ]
            },
            "description": "Answers \"is this process alive?\". Touches no dependency by design, so a database or Redis blip cannot cause an orchestrator to restart otherwise-healthy instances.",
            "auth": {
              "type": "noauth"
            }
          },
          "response": []
        },
        {
          "name": "Readiness probe",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/ready",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "ready"
              ]
            },
            "description": "Answers \"should this process receive traffic?\". PostgreSQL is required and Redis is not, so losing Redis reports `degraded` and still answers 200 while losing PostgreSQL answers 503 and takes the instance out of the load balancer.",
            "auth": {
              "type": "noauth"
            }
          },
          "response": []
        },
        {
          "name": "Build and version metadata",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/version",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "version"
              ]
            },
            "description": "Identifies the service, API version, environment and Node runtime — useful when several revisions run side by side.",
            "auth": {
              "type": "noauth"
            }
          },
          "response": []
        }
      ]
    },
    {
      "name": "Auth",
      "description": "Registration, sessions and password lifecycle. Unauthenticated except where stated; every credential-handling route sits behind a bucket keyed on IP and submitted email.",
      "item": [
        {
          "name": "Read the password policy",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/auth/password-policy",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "auth",
                "password-policy"
              ]
            },
            "description": "The rules a password must satisfy, so a sign-up form can enforce them before submitting rather than round-tripping a 422.",
            "auth": {
              "type": "noauth"
            }
          },
          "response": []
        },
        {
          "name": "Create an account",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/auth/register",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "auth",
                "register"
              ]
            },
            "description": "Creates a user and immediately issues a session. A verification email is queued rather than sent inline, so a slow mail provider cannot slow registration and a failed send cannot roll back a created account. The body is strict: a stray `platformRole` is a loud 422, never a silently ignored privilege escalation.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"customer@example.com\",\n  \"password\": \"MeetFlow!Demo123\",\n  \"firstName\": \"Ada\",\n  \"lastName\": \"Lovelace\",\n  \"phone\": \"+91 98765 43210\",\n  \"timezone\": \"Asia/Kolkata\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "auth": {
              "type": "noauth"
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Captures the session so every other request in this collection is",
                  "// authenticated. Run this first.",
                  "var ok = pm.response.code >= 200 && pm.response.code < 300;",
                  "",
                  "pm.test('a session was issued', function () {",
                  "  pm.expect(ok, 'expected a 2xx response').to.equal(true);",
                  "});",
                  "",
                  "if (ok) {",
                  "  var body = pm.response.json() || {};",
                  "  var data = body.data || {};",
                  "",
                  "  if (data.accessToken) {",
                  "    pm.collectionVariables.set('accessToken', data.accessToken);",
                  "    console.log('accessToken captured; expires in ' + data.expiresIn + 's');",
                  "  }",
                  "  if (data.refreshToken) {",
                  "    pm.collectionVariables.set('refreshToken', data.refreshToken);",
                  "  }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "Sign in",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "auth",
                "login"
              ]
            },
            "description": "Exchanges an email and password for a session. Rate limited on IP *and* submitted email, so neither spraying one password across many accounts nor many passwords at one account gets a free pass.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"customer@example.com\",\n  \"password\": \"MeetFlow!Demo123\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "auth": {
              "type": "noauth"
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Captures the session so every other request in this collection is",
                  "// authenticated. Run this first.",
                  "var ok = pm.response.code >= 200 && pm.response.code < 300;",
                  "",
                  "pm.test('a session was issued', function () {",
                  "  pm.expect(ok, 'expected a 2xx response').to.equal(true);",
                  "});",
                  "",
                  "if (ok) {",
                  "  var body = pm.response.json() || {};",
                  "  var data = body.data || {};",
                  "",
                  "  if (data.accessToken) {",
                  "    pm.collectionVariables.set('accessToken', data.accessToken);",
                  "    console.log('accessToken captured; expires in ' + data.expiresIn + 's');",
                  "  }",
                  "  if (data.refreshToken) {",
                  "    pm.collectionVariables.set('refreshToken', data.refreshToken);",
                  "  }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "Exchange a refresh token for a new session",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/auth/refresh",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "auth",
                "refresh"
              ]
            },
            "description": "The token may travel in the body or in the httpOnly cookie; the body field is therefore optional. Rate limited like the credential routes: this is the endpoint an attacker holding a stolen token would hammer, and reuse detection is cheaper when it is not flooded.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"refreshToken\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "auth": {
              "type": "noauth"
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Captures the session so every other request in this collection is",
                  "// authenticated. Run this first.",
                  "var ok = pm.response.code >= 200 && pm.response.code < 300;",
                  "",
                  "pm.test('a session was issued', function () {",
                  "  pm.expect(ok, 'expected a 2xx response').to.equal(true);",
                  "});",
                  "",
                  "if (ok) {",
                  "  var body = pm.response.json() || {};",
                  "  var data = body.data || {};",
                  "",
                  "  if (data.accessToken) {",
                  "    pm.collectionVariables.set('accessToken', data.accessToken);",
                  "    console.log('accessToken captured; expires in ' + data.expiresIn + 's');",
                  "  }",
                  "  if (data.refreshToken) {",
                  "    pm.collectionVariables.set('refreshToken', data.refreshToken);",
                  "  }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "Revoke one session",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/auth/logout",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "auth",
                "logout"
              ]
            },
            "description": "Revokes the presented refresh token and clears the cookie. Answers 204 whether or not a token was supplied, so a client can always end a session cleanly.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"refreshToken\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "auth": {
              "type": "noauth"
            }
          },
          "response": []
        },
        {
          "name": "Revoke every session for the current user",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/auth/logout-all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "auth",
                "logout-all"
              ]
            },
            "description": "Signs the account out everywhere, on every device."
          },
          "response": []
        },
        {
          "name": "Read the authenticated user",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/auth/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "auth",
                "me"
              ]
            },
            "description": "Returns the user, every ACTIVE membership they hold, and — once a workspace has been selected for the request — the active workspace with the caller's effective permission keys. This is where a client discovers which value to send as X-Business-Id."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Picks a workspace to act in. X-Business-Id is optional when the user",
                  "// belongs to exactly one workspace and required when they belong to",
                  "// several, so capturing it here makes the rest of the collection work",
                  "// either way.",
                  "var ok = pm.response.code === 200;",
                  "",
                  "if (ok) {",
                  "  var body = pm.response.json() || {};",
                  "  var memberships = (body.data || {}).memberships || [];",
                  "",
                  "  if (memberships.length > 0) {",
                  "    pm.collectionVariables.set('businessId', memberships[0].businessId);",
                  "    console.log('businessId set to ' + memberships[0].businessName);",
                  "  }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "Confirm an email address",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/auth/verify-email",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "auth",
                "verify-email"
              ]
            },
            "description": "Consumes the single-use token from the verification email.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"token\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "auth": {
              "type": "noauth"
            }
          },
          "response": []
        },
        {
          "name": "Request a password reset email",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/auth/password-reset/request",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "auth",
                "password-reset",
                "request"
              ]
            },
            "description": "Always answers the same way whether or not the address is registered, so the endpoint cannot be used to enumerate accounts.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"customer@example.com\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "auth": {
              "type": "noauth"
            }
          },
          "response": []
        },
        {
          "name": "Set a new password from a reset token",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/auth/password-reset/confirm",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "auth",
                "password-reset",
                "confirm"
              ]
            },
            "description": "Consumes the single-use token and revokes existing sessions.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"token\": \"\",\n  \"password\": \"MeetFlow!Demo123\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "auth": {
              "type": "noauth"
            }
          },
          "response": []
        },
        {
          "name": "Change the password of the signed-in user",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/auth/change-password",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "auth",
                "change-password"
              ]
            },
            "description": "Requires the current password, and revokes existing sessions on success.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"currentPassword\": \"MeetFlow!Demo123\",\n  \"newPassword\": \"MeetFlow!Demo123\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        }
      ]
    },
    {
      "name": "Workspace",
      "description": "Creating a workspace, and reading or amending the one the caller is acting in — including its booking policy, members and roles.",
      "item": [
        {
          "name": "Create a workspace",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/workspaces",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "workspaces"
              ]
            },
            "description": "Authenticated but not tenant-scoped, and mounted outside the management router for that reason: requiring an existing membership to create your first workspace would make onboarding impossible. The caller becomes its owner.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Example\",\n  \"timezone\": \"Asia/Kolkata\",\n  \"slug\": \"example\",\n  \"description\": \"\",\n  \"industry\": \"\",\n  \"currency\": \"INR\",\n  \"locale\": \"en-IN\",\n  \"websiteUrl\": \"https://example.com\",\n  \"supportEmail\": \"customer@example.com\",\n  \"supportPhone\": \"\",\n  \"createStaffProfile\": false\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Check whether a workspace slug is free",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/workspaces/slug-available?slug=example",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "workspaces",
                "slug-available"
              ],
              "query": [
                {
                  "key": "slug",
                  "value": "example",
                  "disabled": false
                }
              ]
            },
            "description": "Lets a sign-up form report a clash before submitting the whole form."
          },
          "response": []
        },
        {
          "name": "Read the current workspace",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/workspace",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "workspace"
              ]
            },
            "description": "The workspace resolved from the caller's membership. Requires `workspace:read`."
          },
          "response": []
        },
        {
          "name": "Amend the current workspace",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/workspace",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "workspace"
              ]
            },
            "description": "Requires `workspace:update`. `businessId` is absent from the body and always will be: the tenant comes from the membership, so accepting one here would be the client-supplied tenant id the architecture forbids.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Example\",\n  \"description\": \"\",\n  \"industry\": \"\",\n  \"timezone\": \"Asia/Kolkata\",\n  \"currency\": \"INR\",\n  \"locale\": \"en-IN\",\n  \"logoUrl\": \"https://example.com\",\n  \"websiteUrl\": \"https://example.com\",\n  \"supportEmail\": \"customer@example.com\",\n  \"supportPhone\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Read the booking policy",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/workspace/settings",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "workspace",
                "settings"
              ]
            },
            "description": "Slot interval, buffers, notice, horizon, cancellation and reschedule deadlines, approval and waitlist behaviour. Requires `workspace:read`."
          },
          "response": []
        },
        {
          "name": "Amend the booking policy",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/workspace/settings",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "workspace",
                "settings"
              ]
            },
            "description": "Requires `workspace:settings:manage`. Every field is optional so a client can change one value without echoing the rest back.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"slotIntervalMinutes\": 1,\n  \"defaultPreBufferMinutes\": 0,\n  \"defaultPostBufferMinutes\": 0,\n  \"minNoticeMinutes\": 0,\n  \"maxHorizonDays\": 1,\n  \"cancellationDeadlineMinutes\": 0,\n  \"rescheduleDeadlineMinutes\": 0,\n  \"allowCustomerCancel\": false,\n  \"allowCustomerReschedule\": false,\n  \"maxReschedulesPerAppointment\": 0,\n  \"requireApproval\": false,\n  \"maxBookingsPerCustomerPerDay\": 1,\n  \"maxBookingsPerStaffPerDay\": 1,\n  \"noShowGraceMinutes\": 0,\n  \"waitlistEnabled\": false,\n  \"waitlistHoldMinutes\": 1,\n  \"waitlistAutoBook\": false,\n  \"reminderOffsetsMinutes\": [\n    1\n  ],\n  \"branding\": {}\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "List workspace members (superseded)",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/workspace/members",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "workspace",
                "members"
              ]
            },
            "description": "Every membership in the workspace with its role, unfiltered and unpaginated. Requires `members:read`. Superseded by `GET /api/v1/members`, which answers the same question with paging, search and filters; new clients should use that one. This path still works and is unchanged — retiring it is a breaking change and belongs to a release note rather than to a quiet removal — but it will not gain features."
          },
          "response": []
        },
        {
          "name": "List assignable roles",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/workspace/roles",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "workspace",
                "roles"
              ]
            },
            "description": "The roles available in this workspace and the permissions each carries. Requires `roles:read`."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Locations",
      "description": "The sites appointments can be held at.",
      "item": [
        {
          "name": "List locations",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/locations",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "locations"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "20",
                  "disabled": true
                },
                {
                  "key": "isActive",
                  "value": "true",
                  "disabled": true
                },
                {
                  "key": "type",
                  "value": "PHYSICAL",
                  "disabled": true
                }
              ]
            },
            "description": "Requires `locations:read`."
          },
          "response": []
        },
        {
          "name": "Create a location",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/locations",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "locations"
              ]
            },
            "description": "Requires `locations:manage`. `timezone` may be omitted, in which case the location inherits the workspace zone rather than the column's UTC default.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Example\",\n  \"slug\": \"example\",\n  \"type\": \"PHYSICAL\",\n  \"description\": \"\",\n  \"addressLine1\": \"\",\n  \"addressLine2\": \"\",\n  \"city\": \"\",\n  \"state\": \"\",\n  \"postalCode\": \"\",\n  \"countryCode\": \"\",\n  \"timezone\": \"Asia/Kolkata\",\n  \"phone\": \"+91 98765 43210\",\n  \"email\": \"customer@example.com\",\n  \"virtualMeetingUrl\": \"https://example.com\",\n  \"capacity\": 0,\n  \"sortOrder\": 0,\n  \"isActive\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Read one location",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/locations/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "locations",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `locations:read`."
          },
          "response": []
        },
        {
          "name": "Amend a location",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/locations/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "locations",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `locations:manage`. At least one field must be present — an empty patch would write an audit row describing no change at all.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Example\",\n  \"slug\": \"example\",\n  \"type\": \"PHYSICAL\",\n  \"description\": \"\",\n  \"addressLine1\": \"\",\n  \"addressLine2\": \"\",\n  \"city\": \"\",\n  \"state\": \"\",\n  \"postalCode\": \"\",\n  \"countryCode\": \"\",\n  \"timezone\": \"Asia/Kolkata\",\n  \"phone\": \"+91 98765 43210\",\n  \"email\": \"customer@example.com\",\n  \"virtualMeetingUrl\": \"https://example.com\",\n  \"capacity\": 0,\n  \"sortOrder\": 0,\n  \"isActive\": false\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Delete a location",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/locations/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "locations",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `locations:manage`."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Teams",
      "description": "Groups of staff that share an assignment strategy.",
      "item": [
        {
          "name": "List teams",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/teams",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "teams"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "20",
                  "disabled": true
                },
                {
                  "key": "search",
                  "value": "",
                  "disabled": true
                },
                {
                  "key": "isActive",
                  "value": "true",
                  "disabled": true
                },
                {
                  "key": "assignmentStrategy",
                  "value": "ROUND_ROBIN",
                  "disabled": true
                }
              ]
            },
            "description": "Requires `teams:read`."
          },
          "response": []
        },
        {
          "name": "Create a team",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/teams",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "teams"
              ]
            },
            "description": "Requires `teams:manage`. The slug is derived from the name when omitted, and validated rather than silently rewritten when supplied.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Example\",\n  \"slug\": \"example\",\n  \"description\": \"\",\n  \"assignmentStrategy\": \"ROUND_ROBIN\",\n  \"isActive\": false\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Read one team",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/teams/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "teams",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `teams:read`."
          },
          "response": []
        },
        {
          "name": "Amend a team",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/teams/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "teams",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `teams:manage`. At least one field must be present.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Example\",\n  \"slug\": \"example\",\n  \"description\": \"\",\n  \"assignmentStrategy\": \"ROUND_ROBIN\",\n  \"isActive\": false\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Delete a team",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/teams/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "teams",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `teams:manage`."
          },
          "response": []
        },
        {
          "name": "Add a staff member to a team",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/teams/:id/members",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "teams",
                ":id",
                "members"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `teams:manage`. `weight` biases round-robin assignment; `priority` breaks ties, lowest first.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"staffProfileId\": \"00000000-0000-0000-0000-000000000000\",\n  \"weight\": 1,\n  \"priority\": 0\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Amend a team member",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/teams/:id/members/:memberId",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "teams",
                ":id",
                "members",
                ":memberId"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                },
                {
                  "key": "memberId",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `teams:manage`. Setting `isActive` to false takes the member out of rotation while keeping their history and their place in the team — the join row has no soft delete.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"weight\": 1,\n  \"priority\": 0,\n  \"isActive\": false\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Remove a staff member from a team",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/teams/:id/members/:memberId",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "teams",
                ":id",
                "members",
                ":memberId"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                },
                {
                  "key": "memberId",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `teams:manage`."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Staff",
      "description": "Bookable provider profiles and the services they deliver.",
      "item": [
        {
          "name": "List staff profiles",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/staff",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "staff"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "20",
                  "disabled": true
                },
                {
                  "key": "isActive",
                  "value": "true",
                  "disabled": true
                },
                {
                  "key": "isBookable",
                  "value": "true",
                  "disabled": true
                }
              ]
            },
            "description": "Requires `staff:read`."
          },
          "response": []
        },
        {
          "name": "Create a staff profile",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/staff",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "staff"
              ]
            },
            "description": "Requires `staff:manage`. The person is named only by `membershipId`: the membership proves they already belong to this workspace, and the user id is read off that row, so neither `businessId` nor `userId` is accepted.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"membershipId\": \"00000000-0000-0000-0000-000000000000\",\n  \"displayName\": \"Ada Lovelace\",\n  \"title\": \"Example\",\n  \"bio\": \"\",\n  \"avatarUrl\": \"https://example.com\",\n  \"timezone\": \"Asia/Kolkata\",\n  \"color\": \"#4F46E5\",\n  \"defaultLocationId\": \"00000000-0000-0000-0000-000000000000\",\n  \"isBookable\": false,\n  \"preBufferMinutes\": 0,\n  \"postBufferMinutes\": 0,\n  \"minNoticeMinutes\": 0,\n  \"maxDailyAppointments\": 0,\n  \"maxWeeklyAppointments\": 0,\n  \"assignmentWeight\": 1,\n  \"sortOrder\": 0,\n  \"isActive\": false\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Read one staff profile",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/staff/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "staff",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `staff:read`."
          },
          "response": []
        },
        {
          "name": "Amend a staff profile",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/staff/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "staff",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `staff:manage`. `membershipId` cannot be changed: rebinding a profile to a different person would silently reassign their whole appointment history.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"displayName\": \"Ada Lovelace\",\n  \"title\": \"Example\",\n  \"bio\": \"\",\n  \"avatarUrl\": \"https://example.com\",\n  \"timezone\": \"Asia/Kolkata\",\n  \"color\": \"#4F46E5\",\n  \"defaultLocationId\": \"00000000-0000-0000-0000-000000000000\",\n  \"isBookable\": false,\n  \"preBufferMinutes\": 0,\n  \"postBufferMinutes\": 0,\n  \"minNoticeMinutes\": 0,\n  \"maxDailyAppointments\": 0,\n  \"maxWeeklyAppointments\": 0,\n  \"assignmentWeight\": 1,\n  \"sortOrder\": 0,\n  \"isActive\": false\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Delete a staff profile",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/staff/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "staff",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `staff:manage`."
          },
          "response": []
        },
        {
          "name": "List the services a staff member delivers",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/staff/:id/services",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "staff",
                ":id",
                "services"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `staff:read`."
          },
          "response": []
        },
        {
          "name": "Replace the services a staff member delivers",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/staff/:id/services",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "staff",
                ":id",
                "services"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `staff:manage`. PUT, not PATCH: the body is the complete set, and anything absent from it is withdrawn. An empty array is a valid instruction meaning \"no services\".",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"serviceIds\": [\n    \"00000000-0000-0000-0000-000000000000\"\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        }
      ]
    },
    {
      "name": "Members",
      "description": "Who belongs to the workspace, what each of them may do, and how somebody joins or leaves. This is the surface that hands out authority, so what it guarantees is what cannot happen: the last account able to manage roles cannot be demoted, suspended or removed, nobody may edit their own membership or the owners, and a member still holding upcoming appointments cannot be removed out from under them. A role change lands on the next request the affected person makes — effective permissions are read from the membership on every call rather than baked into a token — so a mistake is undone as fast as it was made, and revoking access does not wait for a session to expire. Two paths here are authenticated but not tenant-scoped, and say so.",
      "item": [
        {
          "name": "List workspace members",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/members",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "members"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "20",
                  "disabled": true
                },
                {
                  "key": "search",
                  "value": "",
                  "disabled": true
                },
                {
                  "key": "status",
                  "value": "ACTIVE",
                  "disabled": true
                },
                {
                  "key": "roleId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "includeRemoved",
                  "value": "false",
                  "disabled": true
                }
              ]
            },
            "description": "Requires `members:read`. Supersedes `GET /api/v1/workspace/members`, which still answers but returns an unfiltered, unpaginated array; prefer this one, which adds paging, a search over name and email, and filters on status and role. `includeRemoved` brings back soft-deleted memberships so somebody who left can be found and invited again rather than entered as a stranger."
          },
          "response": []
        },
        {
          "name": "Invite somebody to the workspace",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/members/invite",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "members",
                "invite"
              ]
            },
            "description": "Requires `members:invite`. An invitation names an email address, never a user id: accepting one would turn this endpoint into an oracle for which accounts exist on the platform. An address with no account gets one created in the INVITED state together with a set-password link; an address that already has an account is attached to the workspace and the account itself is left completely untouched, so an invitation can never rewrite somebody's name or credentials. The invitation token is emailed, not returned here. A live membership for that address is a 409, and a previously removed one is re-invited rather than duplicated.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"customer@example.com\",\n  \"roleId\": \"00000000-0000-0000-0000-000000000000\",\n  \"firstName\": \"Ada\",\n  \"lastName\": \"Lovelace\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Change a member's role or status",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/members/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "members",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `members:update`. `status` accepts only ACTIVE and SUSPENDED: INVITED is written by the invitation flow and cleared by acceptance, and REMOVED belongs to the delete route, so neither is a state an operator sets by hand. Three refusals are worth knowing about, and all three answer 409: a caller cannot edit their own membership, nobody can edit the workspace owner's, and the last member holding `roles:manage` cannot be demoted or suspended — that one is the fastest way to lock a workspace out of itself. A role change takes effect on the affected person's next request; their existing token keeps working and simply carries less authority.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"roleId\": \"00000000-0000-0000-0000-000000000000\",\n  \"status\": \"ACTIVE\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Remove a member from the workspace",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/members/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "members",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `members:remove`. A soft delete, so the address can be invited again later and the person's audit history stays attributable. Their staff profile is retired in the same transaction — leaving it live would keep somebody who has left bookable — and the removal is refused with a 409 while they still hold upcoming appointments, because cancelling or reassigning those is a decision somebody has to make rather than a side effect of tidying up a list. Self-removal, removing the owner and removing the last member who can manage roles are all refused for the same reasons as PATCH."
          },
          "response": []
        },
        {
          "name": "Read what one member may do",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/members/:id/permissions",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "members",
                ":id",
                "permissions"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `members:read`. Answers the question a support conversation actually asks — \"why can they not do this?\" — by showing the role grant and the per-member exceptions separately, alongside the effective set the authorisation middleware will enforce."
          },
          "response": []
        },
        {
          "name": "Replace one member's permission exceptions",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/members/:id/permissions",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "members",
                ":id",
                "permissions"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `roles:manage`, deliberately not the weaker `members:update` a Manager holds: a Manager may change who does which job, not what a job is allowed to do. A full replacement rather than a patch — whatever is absent is deleted — because a merge would make \"remove this DENY\" impossible to express. Refused on the caller themselves and on the workspace owner, since a DENY placed there is the quickest lockout in the product.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"overrides\": [\n    {\n      \"permission\": \"workspace:read\",\n      \"effect\": \"GRANT\"\n    }\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "List the invitations addressed to me",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/members/invitations",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "members",
                "invitations"
              ]
            },
            "description": "Authenticated and **not** tenant-scoped: the caller has been invited somewhere but belongs nowhere yet, so there is no membership for tenant resolution to find. No permission is checked either, and none is needed — the only rows this can return are invitations addressed to the caller's own account. Each one carries a freshly issued token, so a lost invitation email cannot strand somebody."
          },
          "response": []
        },
        {
          "name": "Accept an invitation",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/members/accept",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "members",
                "accept"
              ]
            },
            "description": "Authenticated and **not** tenant-scoped, for the reason above. Both halves are required and neither is sufficient alone: the token says which invitation is being accepted, and the session proves the caller is the person it was addressed to. Everything that fails answers the same 404 — a token that is forged, expired, already used, withdrawn, or addressed to somebody else — so the endpoint cannot be used to probe which memberships exist. On success the membership becomes ACTIVE, which also closes the token: a replay finds nothing in the INVITED state to accept.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"token\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        }
      ]
    },
    {
      "name": "Services",
      "description": "The service catalogue and its categories.",
      "item": [
        {
          "name": "List service categories",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/services/categories",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "services",
                "categories"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "20",
                  "disabled": true
                },
                {
                  "key": "isActive",
                  "value": "true",
                  "disabled": true
                }
              ]
            },
            "description": "Requires `services:read`."
          },
          "response": []
        },
        {
          "name": "Create a service category",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/services/categories",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "services",
                "categories"
              ]
            },
            "description": "Requires `services:manage`.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Example\",\n  \"slug\": \"example\",\n  \"description\": \"\",\n  \"color\": \"#4F46E5\",\n  \"sortOrder\": 0,\n  \"isActive\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Amend a service category",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/services/categories/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "services",
                "categories",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `services:manage`. At least one field must be present.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Example\",\n  \"slug\": \"example\",\n  \"description\": \"\",\n  \"color\": \"#4F46E5\",\n  \"sortOrder\": 0,\n  \"isActive\": false\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Delete a service category",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/services/categories/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "services",
                "categories",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `services:manage`."
          },
          "response": []
        },
        {
          "name": "List services",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/services",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "services"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "20",
                  "disabled": true
                },
                {
                  "key": "isActive",
                  "value": "true",
                  "disabled": true
                },
                {
                  "key": "isPublic",
                  "value": "true",
                  "disabled": true
                },
                {
                  "key": "categoryId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                }
              ]
            },
            "description": "Requires `services:read`."
          },
          "response": []
        },
        {
          "name": "Create a service",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/services",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "services"
              ]
            },
            "description": "Requires `services:manage`. `priceAmount` is in the smallest currency unit — 1250 for ₹12.50 — and a decimal is rejected rather than coerced. The nullable policy overrides (`preBufferMinutes`, `minNoticeMinutes`, `maxHorizonDays`, `slotIntervalMinutes`, `maxPerCustomerPerDay`) mean \"inherit the workspace setting\" when null and \"explicitly none\" when 0.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Example\",\n  \"slug\": \"example\",\n  \"categoryId\": \"00000000-0000-0000-0000-000000000000\",\n  \"description\": \"\",\n  \"durationMinutes\": 1,\n  \"preBufferMinutes\": 0,\n  \"postBufferMinutes\": 0,\n  \"priceAmount\": 0,\n  \"capacity\": 1,\n  \"minNoticeMinutes\": 0,\n  \"maxHorizonDays\": 1,\n  \"slotIntervalMinutes\": 1,\n  \"maxPerCustomerPerDay\": 1,\n  \"requiresApproval\": false,\n  \"assignmentStrategy\": \"SMART_MATCH\",\n  \"color\": \"#4F46E5\",\n  \"isPublic\": true,\n  \"isActive\": true,\n  \"sortOrder\": 0\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Read one service",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/services/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "services",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `services:read`."
          },
          "response": []
        },
        {
          "name": "Amend a service",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/services/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "services",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `services:manage`. At least one field must be present.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Example\",\n  \"slug\": \"example\",\n  \"categoryId\": \"00000000-0000-0000-0000-000000000000\",\n  \"description\": \"\",\n  \"durationMinutes\": 1,\n  \"preBufferMinutes\": 0,\n  \"postBufferMinutes\": 0,\n  \"priceAmount\": 0,\n  \"capacity\": 1,\n  \"minNoticeMinutes\": 0,\n  \"maxHorizonDays\": 1,\n  \"slotIntervalMinutes\": 1,\n  \"maxPerCustomerPerDay\": 1,\n  \"requiresApproval\": false,\n  \"assignmentStrategy\": \"ROUND_ROBIN\",\n  \"color\": \"#4F46E5\",\n  \"isPublic\": false,\n  \"isActive\": false,\n  \"sortOrder\": 0\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Delete a service",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/services/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "services",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `services:manage`."
          },
          "response": []
        },
        {
          "name": "Replace the staff who deliver a service",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/services/:id/staff",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "services",
                ":id",
                "staff"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `services:manage`. PUT, not PATCH: the body is the complete set the caller wants to end up with. A repeated id is a 422 rather than a silent deduplication, because it would collide with the unique index on the join table.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"staffProfileIds\": [\n    \"00000000-0000-0000-0000-000000000000\"\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Replace the locations a service is offered at",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/services/:id/locations",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "services",
                ":id",
                "locations"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `services:manage`. An empty array is meaningful: no rows means the service is offered everywhere.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"locationIds\": [\n    \"00000000-0000-0000-0000-000000000000\"\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        }
      ]
    },
    {
      "name": "Resources",
      "description": "Rooms and equipment, and the resources each service reserves.",
      "item": [
        {
          "name": "List resources",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/resources",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "resources"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "20",
                  "disabled": true
                },
                {
                  "key": "isActive",
                  "value": "true",
                  "disabled": true
                },
                {
                  "key": "type",
                  "value": "ROOM",
                  "disabled": true
                },
                {
                  "key": "locationId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                }
              ]
            },
            "description": "Requires `resources:read`."
          },
          "response": []
        },
        {
          "name": "Create a resource",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/resources",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "resources"
              ]
            },
            "description": "Requires `resources:manage`. A null `locationId` means the resource is mobile and travels with the appointment; `capacity` is how many appointments may hold it at the same instant.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Example\",\n  \"slug\": \"example\",\n  \"type\": \"ROOM\",\n  \"locationId\": \"00000000-0000-0000-0000-000000000000\",\n  \"description\": \"\",\n  \"capacity\": 1,\n  \"color\": \"#4F46E5\",\n  \"isActive\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Read one resource",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/resources/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "resources",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `resources:read`."
          },
          "response": []
        },
        {
          "name": "Amend a resource",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/resources/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "resources",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `resources:manage`. At least one field must be present.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Example\",\n  \"slug\": \"example\",\n  \"type\": \"ROOM\",\n  \"locationId\": \"00000000-0000-0000-0000-000000000000\",\n  \"description\": \"\",\n  \"capacity\": 1,\n  \"color\": \"#4F46E5\",\n  \"isActive\": false\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Delete a resource",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/resources/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "resources",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `resources:manage`."
          },
          "response": []
        },
        {
          "name": "List the resources a service requires",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/resources/requirements/service/:serviceId",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "resources",
                "requirements",
                "service",
                ":serviceId"
              ],
              "variable": [
                {
                  "key": "serviceId",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `resources:read`. These live under /resources rather than /services because they are edited from the resource-planning screen: a manager who may not touch the catalogue can still say which room a service needs."
          },
          "response": []
        },
        {
          "name": "Replace the resources a service requires",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/resources/requirements/service/:serviceId",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "resources",
                "requirements",
                "service",
                ":serviceId"
              ],
              "variable": [
                {
                  "key": "serviceId",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `resources:manage`. Each row names exactly one of `resourceId` (this specific resource) or `resourceType` (any free resource of that type) — never both, never neither. An optional row is reserved when possible and skipped when not, so a nicety never blocks a booking.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"requirements\": [\n    {\n      \"resourceId\": \"00000000-0000-0000-0000-000000000000\",\n      \"resourceType\": \"ROOM\",\n      \"quantity\": 1,\n      \"isRequired\": true\n    }\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        }
      ]
    },
    {
      "name": "Customers",
      "description": "The workspace address book and its booking history.",
      "item": [
        {
          "name": "List customers",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/customers",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "customers"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "20",
                  "disabled": true
                },
                {
                  "key": "search",
                  "value": "",
                  "disabled": true
                },
                {
                  "key": "status",
                  "value": "ACTIVE",
                  "disabled": true
                },
                {
                  "key": "tag",
                  "value": "",
                  "disabled": true
                }
              ]
            },
            "description": "Requires `customers:read`. `search` matches first name, last name and email."
          },
          "response": []
        },
        {
          "name": "Create a customer",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/customers",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "customers"
              ]
            },
            "description": "Requires `customers:manage`, and additionally `customers:notes:manage` when the body carries `notes` — that rule depends on the payload, so the service enforces it per request rather than the router. Booking counters are read-only and rejected if sent.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"firstName\": \"Ada\",\n  \"lastName\": \"Lovelace\",\n  \"email\": \"customer@example.com\",\n  \"phone\": \"+91 98765 43210\",\n  \"timezone\": \"Asia/Kolkata\",\n  \"locale\": \"en-IN\",\n  \"notes\": \"\",\n  \"tags\": [\n    \"\"\n  ],\n  \"preferredStaffProfileId\": \"00000000-0000-0000-0000-000000000000\",\n  \"preferredLocationId\": \"00000000-0000-0000-0000-000000000000\",\n  \"communicationPreferences\": {\n    \"emailEnabled\": true,\n    \"smsEnabled\": false,\n    \"marketingOptIn\": false,\n    \"reminderOffsetsMinutes\": [\n      0\n    ]\n  },\n  \"status\": \"ACTIVE\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Read one customer",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/customers/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "customers",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `customers:read`."
          },
          "response": []
        },
        {
          "name": "Amend a customer",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/customers/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "customers",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `customers:manage`, plus `customers:notes:manage` when the patch carries `notes`. `communicationPreferences` is merged over the stored object rather than replacing it, so omitted flags keep their value.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"firstName\": \"Ada\",\n  \"lastName\": \"Lovelace\",\n  \"email\": \"customer@example.com\",\n  \"phone\": \"+91 98765 43210\",\n  \"timezone\": \"Asia/Kolkata\",\n  \"locale\": \"en-IN\",\n  \"notes\": \"\",\n  \"tags\": [\n    \"\"\n  ],\n  \"preferredStaffProfileId\": \"00000000-0000-0000-0000-000000000000\",\n  \"preferredLocationId\": \"00000000-0000-0000-0000-000000000000\",\n  \"communicationPreferences\": {\n    \"emailEnabled\": false,\n    \"smsEnabled\": false,\n    \"marketingOptIn\": false,\n    \"reminderOffsetsMinutes\": [\n      0\n    ]\n  },\n  \"status\": \"ACTIVE\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Delete a customer",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/customers/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "customers",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `customers:manage`."
          },
          "response": []
        },
        {
          "name": "List a customer's appointments",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/customers/:id/appointments",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "customers",
                ":id",
                "appointments"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "20",
                  "disabled": true
                }
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `customers:read`."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Availability",
      "description": "The rota: opening hours, per-staff weekly rules, one-off overrides, holidays and blackout periods.",
      "item": [
        {
          "name": "List opening hours",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/availability/business-hours",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "availability",
                "business-hours"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "20",
                  "disabled": true
                },
                {
                  "key": "locationId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "isActive",
                  "value": "true",
                  "disabled": true
                }
              ]
            },
            "description": "Requires `availability:read`. Omitting `locationId` returns the business-wide set — the same set PUT replaces when its body carries no locationId, so one GET round-trips into one PUT."
          },
          "response": []
        },
        {
          "name": "Replace opening hours",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/availability/business-hours",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "availability",
                "business-hours"
              ]
            },
            "description": "Requires `availability:manage`. PUT, not PATCH: the body is the complete week for the named scope, and an empty array means \"this scope has no opening hours at all\". Overlapping active windows are rejected with the offending index. Windows are authored as `HH:mm` wall-clock times and stored as minutes from local midnight. An end at or before the start is read as running into the next day, so 22:00–02:00 is a single overnight window. `24:00` is midnight at the *end* of a day and may only close one.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"locationId\": null,\n  \"hours\": [\n    {\n      \"dayOfWeek\": 0,\n      \"startTime\": \"09:00\",\n      \"endTime\": \"17:00\",\n      \"isActive\": true\n    }\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "List a staff member's weekly availability rules",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/availability/staff/:staffProfileId/rules",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "availability",
                "staff",
                ":staffProfileId",
                "rules"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "20",
                  "disabled": true
                },
                {
                  "key": "isActive",
                  "value": "true",
                  "disabled": true
                }
              ],
              "variable": [
                {
                  "key": "staffProfileId",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `availability:read`."
          },
          "response": []
        },
        {
          "name": "Replace a staff member's weekly availability rules",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/availability/staff/:staffProfileId/rules",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "availability",
                "staff",
                ":staffProfileId",
                "rules"
              ],
              "variable": [
                {
                  "key": "staffProfileId",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `availability:manage` or `availability:manage:own`; the service then refuses anything that is not the caller's own row, because whose rota is being edited is a property of the request rather than of the route. Overlap detection is deliberately blind to `locationId`: one person cannot be at two sites at once. Windows are authored as `HH:mm` wall-clock times and stored as minutes from local midnight. An end at or before the start is read as running into the next day, so 22:00–02:00 is a single overnight window. `24:00` is midnight at the *end* of a day and may only close one.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"rules\": [\n    {\n      \"dayOfWeek\": 0,\n      \"startTime\": \"09:00\",\n      \"endTime\": \"17:00\",\n      \"locationId\": null,\n      \"effectiveFrom\": null,\n      \"effectiveTo\": null,\n      \"isActive\": true\n    }\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "List availability overrides",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/availability/overrides",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "availability",
                "overrides"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "20",
                  "disabled": true
                },
                {
                  "key": "staffProfileId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "scope",
                  "value": "BUSINESS",
                  "disabled": true
                },
                {
                  "key": "from",
                  "value": "2025-03-01",
                  "disabled": true
                },
                {
                  "key": "to",
                  "value": "2025-03-01",
                  "disabled": true
                }
              ]
            },
            "description": "Requires `availability:read`."
          },
          "response": []
        },
        {
          "name": "Create an availability override",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/availability/overrides",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "availability",
                "overrides"
              ]
            },
            "description": "Requires `availability:manage` or `availability:manage:own`. `isAvailable: false` removes time the recurring rules offer; true adds a window. The scope decides which target ids are legal: BUSINESS names none, LOCATION names only a location, STAFF names a staff profile and may narrow to a location, RESOURCE likewise. Give both `startTime` and `endTime` for a partial day, or neither for a whole one.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"scope\": \"BUSINESS\",\n  \"staffProfileId\": null,\n  \"locationId\": null,\n  \"resourceId\": null,\n  \"date\": \"2025-03-01\",\n  \"isAvailable\": false,\n  \"startTime\": null,\n  \"endTime\": null,\n  \"reason\": null,\n  \"note\": null\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Delete an availability override",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/availability/overrides/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "availability",
                "overrides",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `availability:manage` or `availability:manage:own`."
          },
          "response": []
        },
        {
          "name": "List holidays",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/availability/holidays",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "availability",
                "holidays"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "20",
                  "disabled": true
                },
                {
                  "key": "locationId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "isActive",
                  "value": "true",
                  "disabled": true
                },
                {
                  "key": "from",
                  "value": "2025-03-01",
                  "disabled": true
                },
                {
                  "key": "to",
                  "value": "2025-03-01",
                  "disabled": true
                }
              ]
            },
            "description": "Requires `availability:read`."
          },
          "response": []
        },
        {
          "name": "Create a holiday",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/availability/holidays",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "availability",
                "holidays"
              ]
            },
            "description": "Requires `availability:manage`. A null `locationId` means every location observes it; `isRecurringAnnually` repeats it on the same month and day thereafter; `closesBusiness: false` labels the day for customers without removing any availability.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Example\",\n  \"date\": \"2025-03-01\",\n  \"locationId\": null,\n  \"isRecurringAnnually\": false,\n  \"closesBusiness\": true,\n  \"isActive\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Delete a holiday",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/availability/holidays/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "availability",
                "holidays",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `availability:manage`."
          },
          "response": []
        },
        {
          "name": "List blackout periods",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/availability/blackouts",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "availability",
                "blackouts"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "20",
                  "disabled": true
                },
                {
                  "key": "scope",
                  "value": "BUSINESS",
                  "disabled": true
                },
                {
                  "key": "staffProfileId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "locationId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "resourceId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "from",
                  "value": "2025-03-01T09:00:00Z",
                  "disabled": true
                },
                {
                  "key": "to",
                  "value": "2025-03-01T09:00:00Z",
                  "disabled": true
                }
              ]
            },
            "description": "Requires `availability:read`."
          },
          "response": []
        },
        {
          "name": "Create a blackout period",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/availability/blackouts",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "availability",
                "blackouts"
              ]
            },
            "description": "Requires `availability:manage`. Unlike an override, a blackout is bounded by two instants rather than a calendar day, so both must carry their offset. The same scope/target rule as overrides applies.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"scope\": \"BUSINESS\",\n  \"staffProfileId\": null,\n  \"locationId\": null,\n  \"resourceId\": null,\n  \"startsAt\": \"2025-03-01T09:00:00Z\",\n  \"endsAt\": \"2025-03-01T09:00:00Z\",\n  \"reason\": \"CUSTOM\",\n  \"note\": null\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Delete a blackout period",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/availability/blackouts/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "availability",
                "blackouts",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `availability:manage`."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Booking Links",
      "description": "The public booking pages a workspace publishes.",
      "item": [
        {
          "name": "List booking links",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/booking-links",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "booking-links"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "20",
                  "disabled": true
                },
                {
                  "key": "isActive",
                  "value": "true",
                  "disabled": true
                },
                {
                  "key": "type",
                  "value": "SINGLE_SERVICE",
                  "disabled": true
                },
                {
                  "key": "search",
                  "value": "",
                  "disabled": true
                }
              ]
            },
            "description": "Requires `booking-links:read`."
          },
          "response": []
        },
        {
          "name": "Create a booking link",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/booking-links",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "booking-links"
              ]
            },
            "description": "Requires `booking-links:manage`. The `type` decides which target the link must name: SINGLE_SERVICE names `serviceId`, TEAM names `teamId`, STAFF names `staffProfileId`, and CATALOG names none because its offering comes from `serviceIds`. Naming a target the type does not use is a 422, not a stored intention the public page will never act on. The slug *is* the public URL, so a supplied one is validated rather than rewritten.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Example\",\n  \"slug\": \"example\",\n  \"description\": \"\",\n  \"type\": \"CATALOG\",\n  \"serviceId\": \"00000000-0000-0000-0000-000000000000\",\n  \"teamId\": \"00000000-0000-0000-0000-000000000000\",\n  \"staffProfileId\": \"00000000-0000-0000-0000-000000000000\",\n  \"locationId\": \"00000000-0000-0000-0000-000000000000\",\n  \"serviceIds\": [\n    \"00000000-0000-0000-0000-000000000000\"\n  ],\n  \"allowStaffSelection\": true,\n  \"requiresApproval\": false,\n  \"customQuestions\": [],\n  \"branding\": {},\n  \"maxBookingsTotal\": 0,\n  \"expiresAt\": \"2025-03-01T09:00:00Z\",\n  \"isActive\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Read one booking link",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/booking-links/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "booking-links",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `booking-links:read`."
          },
          "response": []
        },
        {
          "name": "Amend a booking link",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/booking-links/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "booking-links",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `booking-links:manage`. Only contradictions *within* the patch are caught here; whether the merged row still satisfies the type/target rule depends on the stored target, so the service re-checks the result before writing.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Example\",\n  \"slug\": \"example\",\n  \"description\": \"\",\n  \"type\": \"SINGLE_SERVICE\",\n  \"serviceId\": \"00000000-0000-0000-0000-000000000000\",\n  \"teamId\": \"00000000-0000-0000-0000-000000000000\",\n  \"staffProfileId\": \"00000000-0000-0000-0000-000000000000\",\n  \"locationId\": \"00000000-0000-0000-0000-000000000000\",\n  \"allowStaffSelection\": false,\n  \"requiresApproval\": false,\n  \"customQuestions\": [\n    {\n      \"key\": \"example_key\",\n      \"label\": \"Example question\",\n      \"type\": \"TEXT\",\n      \"required\": false,\n      \"options\": []\n    }\n  ],\n  \"branding\": {},\n  \"maxBookingsTotal\": 0,\n  \"expiresAt\": \"2025-03-01T09:00:00Z\",\n  \"isActive\": false\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Delete a booking link",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/booking-links/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "booking-links",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `booking-links:manage`."
          },
          "response": []
        },
        {
          "name": "Replace the services a booking link offers",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/booking-links/:id/services",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "booking-links",
                ":id",
                "services"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `booking-links:manage`. PUT, not PATCH: the body is the complete set, and the array order is the order the public page lists them in.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"serviceIds\": [\n    \"00000000-0000-0000-0000-000000000000\"\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        }
      ]
    },
    {
      "name": "Appointments",
      "description": "The diary. Booking, amending, and the status lifecycle — approve, reject, check in, complete and no-show.",
      "item": [
        {
          "name": "List appointments",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/appointments",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "appointments"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "20",
                  "disabled": true
                },
                {
                  "key": "status",
                  "value": "PENDING",
                  "disabled": true
                },
                {
                  "key": "from",
                  "value": "2025-03-01T09:00:00Z",
                  "disabled": true
                },
                {
                  "key": "to",
                  "value": "2025-03-01T09:00:00Z",
                  "disabled": true
                },
                {
                  "key": "staffProfileId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "serviceId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "locationId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "customerId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "q",
                  "value": "",
                  "disabled": true
                }
              ]
            },
            "description": "Requires `appointments:read` or `appointments:read:own`. The router admits both and the query layer narrows to the caller's own diary, answering 404 — never 403 — for anything outside it. `from`/`to` bound a window rather than a start time: an appointment that began before the window and is still running belongs in it."
          },
          "response": []
        },
        {
          "name": "Book an appointment",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              },
              {
                "key": "X-Idempotency-Key",
                "value": "booking-2f7c1e90-1a2b-4c3d-9e8f-0a1b2c3d4e5f",
                "type": "text",
                "description": "A client-chosen key that makes a booking request safe to retry. A repeat carrying the same key answers with the original booking instead of creating a second one — the management route reports this as `meta.replayed`, the public route by answering 200 rather than 201.",
                "disabled": true
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/appointments",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "appointments"
              ]
            },
            "description": "Requires `appointments:create`. `staffProfileId` is required here, unlike on the public surface: staff-side booking is deliberate assignment, not Smart Match, and the slot search has already named the provider. `internalNotes` is deliberately not accepted — the private operator note is governed by `appointments:notes:manage` and is written through PATCH /{id}. Send X-Idempotency-Key to make a retry safe.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"serviceId\": \"00000000-0000-0000-0000-000000000000\",\n  \"staffProfileId\": \"00000000-0000-0000-0000-000000000000\",\n  \"locationId\": \"00000000-0000-0000-0000-000000000000\",\n  \"startsAt\": \"2025-03-01T09:00:00Z\",\n  \"timezone\": \"Asia/Kolkata\",\n  \"customer\": {\n    \"id\": \"00000000-0000-0000-0000-000000000000\",\n    \"firstName\": \"Ada\",\n    \"lastName\": \"Lovelace\",\n    \"email\": \"customer@example.com\",\n    \"phone\": \"+91 98765 43210\"\n  },\n  \"customerNotes\": \"\",\n  \"answers\": {}\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Read the diary as calendar events",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/appointments/calendar?from=2025-03-01T09:00:00Z&to=2025-03-01T09:00:00Z",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "appointments",
                "calendar"
              ],
              "query": [
                {
                  "key": "status",
                  "value": "PENDING",
                  "disabled": true
                },
                {
                  "key": "from",
                  "value": "2025-03-01T09:00:00Z",
                  "disabled": false
                },
                {
                  "key": "to",
                  "value": "2025-03-01T09:00:00Z",
                  "disabled": false
                },
                {
                  "key": "staffProfileId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "serviceId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "locationId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "customerId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "q",
                  "value": "",
                  "disabled": true
                }
              ]
            },
            "description": "Requires `appointments:read` or `appointments:read:own`. `from` and `to` are required here, unlike on the list: a calendar view always has a visible window, so demanding one costs the client nothing and stops a bare request from scanning a workspace's entire history. The window may span at most 92 days."
          },
          "response": []
        },
        {
          "name": "Search bookable slots",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/appointments/availability/slots?serviceId=00000000-0000-0000-0000-000000000000&fromDate=2025-03-01&toDate=2025-03-01&timezone=Asia/Kolkata",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "appointments",
                "availability",
                "slots"
              ],
              "query": [
                {
                  "key": "serviceId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": false
                },
                {
                  "key": "staffProfileId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "locationId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "fromDate",
                  "value": "2025-03-01",
                  "disabled": false
                },
                {
                  "key": "toDate",
                  "value": "2025-03-01",
                  "disabled": false
                },
                {
                  "key": "timezone",
                  "value": "Asia/Kolkata",
                  "disabled": false
                },
                {
                  "key": "explain",
                  "value": "true",
                  "disabled": true
                }
              ]
            },
            "description": "Requires `availability:read` rather than a diary permission: reading what *could* be booked is an availability question. `timezone` is the zone the requested calendar days are read in, and `explain=true` returns the Smart Match scores behind each provider choice. The result names the provider for each offered time, which is what POST /api/v1/appointments then requires."
          },
          "response": []
        },
        {
          "name": "Read one appointment",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/appointments/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "appointments",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `appointments:read` or `appointments:read:own`."
          },
          "response": []
        },
        {
          "name": "Amend an appointment's notes and title",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/appointments/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "appointments",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `appointments:update` or `appointments:notes:manage`; which one a given request needs depends on the fields it carries, so the router admits either and the service enforces per field. Times and status are absent by design: moving an appointment must go through POST /{id}/reschedule, which re-verifies the slot and moves the underlying reservations, and status must go through the lifecycle routes, which enforce the state machine.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"title\": \"Example\",\n  \"customerNotes\": \"\",\n  \"internalNotes\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Move an appointment",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/appointments/:id/reschedule",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "appointments",
                ":id",
                "reschedule"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `appointments:reschedule`. Omitting `staffProfileId` keeps the current provider; the lifecycle re-verifies the slot and moves the staff and resource reservations either way. A lost race answers 409 SLOT_UNAVAILABLE.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"startsAt\": \"2025-03-01T09:00:00Z\",\n  \"staffProfileId\": \"00000000-0000-0000-0000-000000000000\",\n  \"locationId\": \"00000000-0000-0000-0000-000000000000\",\n  \"reason\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Cancel an appointment",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/appointments/:id/cancel",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "appointments",
                ":id",
                "cancel"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `appointments:cancel`. The optional reason is copied into the status history and into the customer's email.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Approve a pending appointment",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/appointments/:id/approve",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "appointments",
                ":id",
                "approve"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `appointments:approve`. The body is empty and still validated strictly, so a client that thinks it is sending something is told that it is not.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Reject a pending appointment",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/appointments/:id/reject",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "appointments",
                ":id",
                "reject"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `appointments:approve` — rejecting is the other half of approving, so a member who may admit a pending booking may also turn it away.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Check a customer in",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/appointments/:id/check-in",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "appointments",
                ":id",
                "check-in"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `appointments:complete`: check-in is the first step of seeing an appointment through, and that is the permission the STAFF role holds for the appointments it runs.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Complete an appointment",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/appointments/:id/complete",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "appointments",
                ":id",
                "complete"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `appointments:complete`.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Mark an appointment as a no-show",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/appointments/:id/no-show",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "appointments",
                ":id",
                "no-show"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `appointments:no-show`.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        }
      ]
    },
    {
      "name": "Waitlist",
      "description": "Customers waiting for a slot, and converting them into one.",
      "item": [
        {
          "name": "List waitlist entries",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/waitlist",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "waitlist"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "20",
                  "disabled": true
                },
                {
                  "key": "status",
                  "value": "ACTIVE",
                  "disabled": true
                },
                {
                  "key": "serviceId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                }
              ]
            },
            "description": "Requires `waitlist:read`."
          },
          "response": []
        },
        {
          "name": "Add a customer to the waitlist",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/waitlist",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "waitlist"
              ]
            },
            "description": "Requires `waitlist:manage`. The desired window is a pair of local dates plus a daily minute range, read in `timezone` — which defaults to the customer's own zone when omitted. `status`, hold fields and `publicId` are owned by the matcher and the lifecycle, so sending any of them is a 422 rather than a way to hand yourself a hold.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"customerId\": \"00000000-0000-0000-0000-000000000000\",\n  \"serviceId\": \"00000000-0000-0000-0000-000000000000\",\n  \"staffProfileId\": \"00000000-0000-0000-0000-000000000000\",\n  \"locationId\": \"00000000-0000-0000-0000-000000000000\",\n  \"earliestDate\": \"2025-03-01\",\n  \"latestDate\": \"2025-03-01\",\n  \"earliestMinute\": 0,\n  \"latestMinute\": 1439,\n  \"daysOfWeek\": [],\n  \"timezone\": \"Asia/Kolkata\",\n  \"priority\": 100,\n  \"notifyChannel\": \"EMAIL\",\n  \"expiresAt\": \"2025-03-01T09:00:00Z\",\n  \"note\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Read one waitlist entry",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/waitlist/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "waitlist",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `waitlist:read`."
          },
          "response": []
        },
        {
          "name": "Amend a waitlist entry",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/waitlist/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "waitlist",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `waitlist:manage`. `customerId` and `serviceId` cannot be changed: either would turn this into a different request, which the partial unique index makes a collision rather than an edit. Moving a person to another service is a new entry.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"staffProfileId\": \"00000000-0000-0000-0000-000000000000\",\n  \"locationId\": \"00000000-0000-0000-0000-000000000000\",\n  \"earliestDate\": \"2025-03-01\",\n  \"latestDate\": \"2025-03-01\",\n  \"earliestMinute\": 0,\n  \"latestMinute\": 1,\n  \"daysOfWeek\": [\n    0\n  ],\n  \"timezone\": \"Asia/Kolkata\",\n  \"priority\": 0,\n  \"notifyChannel\": \"EMAIL\",\n  \"expiresAt\": \"2025-03-01T09:00:00Z\",\n  \"note\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Delete a waitlist entry",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/waitlist/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "waitlist",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `waitlist:manage`."
          },
          "response": []
        },
        {
          "name": "Re-offer a slot to a waitlisted customer",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/waitlist/:id/notify",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "waitlist",
                ":id",
                "notify"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `waitlist:manage`. The body is empty and validated strictly.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Convert a waitlist entry into a booking",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/waitlist/:id/convert",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "waitlist",
                ":id",
                "convert"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `waitlist:manage`, which the permission catalogue defines as managing *and converting* entries. Only the instant is accepted: the service, the customer and any staff or location preference are read from the entry, so a conversion cannot quietly book something other than what the customer asked for. Answers 201 because the meaningful outcome is a new appointment.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"startsAt\": \"2025-03-01T09:00:00Z\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        }
      ]
    },
    {
      "name": "Webhooks",
      "description": "Outbound event delivery: where a workspace wants to be told when its diary changes. Registering an endpoint points a workspace's event stream at a server of the registrant's choosing, which is a materially larger act than reading delivery history, so the built-in roles grant the two separately and only an owner may register one. The signing secret is returned exactly once, in the creation response, and is readable nowhere else in the API — losing it means replacing the endpoint. Payloads carry opaque identifiers and never a customer name, address or phone number, so a mistyped URL leaks ids rather than a person.",
      "item": [
        {
          "name": "List webhook endpoints",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/webhooks",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "webhooks"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "20",
                  "disabled": true
                },
                {
                  "key": "isActive",
                  "value": "true",
                  "disabled": true
                },
                {
                  "key": "event",
                  "value": "*",
                  "disabled": true
                }
              ]
            },
            "description": "Requires `webhooks:read`. `event` narrows to the endpoints that would be notified of one event, wildcard subscribers included, which is how to answer \"who hears about a cancellation?\" without reading every row."
          },
          "response": []
        },
        {
          "name": "Register a webhook endpoint",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/webhooks",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "webhooks"
              ]
            },
            "description": "Requires `webhooks:manage`, which the built-in roles grant to owners alone: pointing a workspace's event stream at an arbitrary host is an owner-level act. **The response is the only place the signing secret ever appears** — it is excluded from every read by the model default scope, so a client that does not store it now must delete the endpoint and register another. Omitting `events` subscribes to everything, including events added later. A workspace is capped at 20 endpoints, because each one is another insert inside the transaction that takes a booking.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"url\": \"https://example.com\",\n  \"description\": \"\",\n  \"events\": [\n    \"*\"\n  ],\n  \"isActive\": false\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Read one webhook endpoint",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/webhooks/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "webhooks",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `webhooks:read`. Includes the most recent deliveries for triage."
          },
          "response": []
        },
        {
          "name": "Amend a webhook endpoint",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/webhooks/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "webhooks",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `webhooks:manage`. The signing secret is not amendable and is absent from the body: a caller-supplied secret would be a caller-chosen one, and rotating a secret needs an overlap window rather than a field on a PATCH. Re-enabling an endpoint the delivery worker disabled after repeated failures also clears its failure counter, so the next failure starts the count again rather than tripping the limit immediately.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"url\": \"https://example.com\",\n  \"description\": \"\",\n  \"events\": [\n    \"*\"\n  ],\n  \"isActive\": false\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Delete a webhook endpoint",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/webhooks/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "webhooks",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `webhooks:manage`. A soft delete, so the delivery history stays readable and keeps pointing at a real endpoint: withdrawing a subscription must not erase the record of what was already sent where. Anything still queued for it is marked CANCELLED by the worker rather than quietly dropped."
          },
          "response": []
        },
        {
          "name": "Send a test event",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/webhooks/:id/test",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "webhooks",
                ":id",
                "test"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `webhooks:manage` rather than `webhooks:read`: nothing about the endpoint changes, but the server opens an outbound connection to a customer-supplied URL, which is a write to the outside world. The `webhook.test` event is not subscribable, so a wildcard subscriber is never woken by somebody else's connectivity check. Answers 201 once the delivery is queued — the attempt has not happened yet, and its outcome shows up in the delivery history like any other event. A disabled endpoint is refused with 409 rather than accepted: the worker cancels anything addressed to one, so a 201 here would be the least useful possible answer to \"is this endpoint working?\"."
          },
          "response": []
        },
        {
          "name": "List delivery attempts",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/webhooks/:id/deliveries",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "webhooks",
                ":id",
                "deliveries"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "20",
                  "disabled": true
                },
                {
                  "key": "status",
                  "value": "PENDING",
                  "disabled": true
                },
                {
                  "key": "event",
                  "value": "",
                  "disabled": true
                }
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `webhooks:read`. The debugging surface: status, attempt count, the response code and body the far end returned, and the error text when it did not answer at all. `eventId` is stable across every endpoint notified of one occurrence, so a single event can be traced through several subscribers."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Analytics",
      "description": "Aggregations over the appointment table.",
      "item": [
        {
          "name": "Headline figures for a period",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/analytics/overview?from=2025-03-01&to=2025-03-01",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "analytics",
                "overview"
              ],
              "query": [
                {
                  "key": "from",
                  "value": "2025-03-01",
                  "disabled": false
                },
                {
                  "key": "to",
                  "value": "2025-03-01",
                  "disabled": false
                },
                {
                  "key": "locationId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "staffProfileId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                }
              ]
            },
            "description": "The window is a pair of inclusive calendar dates, not instants: \"1 March\" means 1 March where the business is, resolved against the workspace timezone inside PostgreSQL. Both `from` and `to` are required — an analytics view always has a visible period, and demanding one stops a bare request from aggregating a workspace's entire history. Neither `locationId` nor `staffProfileId` is a tenant selector: both narrow a query already filtered by the caller's proven workspace, so a foreign id reports zeros. Requires `analytics:read`."
          },
          "response": []
        },
        {
          "name": "Booking volume over time",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/analytics/trends?from=2025-03-01&to=2025-03-01",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "analytics",
                "trends"
              ],
              "query": [
                {
                  "key": "from",
                  "value": "2025-03-01",
                  "disabled": false
                },
                {
                  "key": "to",
                  "value": "2025-03-01",
                  "disabled": false
                },
                {
                  "key": "locationId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "staffProfileId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                }
              ]
            },
            "description": "The window is a pair of inclusive calendar dates, not instants: \"1 March\" means 1 March where the business is, resolved against the workspace timezone inside PostgreSQL. Both `from` and `to` are required — an analytics view always has a visible period, and demanding one stops a bare request from aggregating a workspace's entire history. Neither `locationId` nor `staffProfileId` is a tenant selector: both narrow a query already filtered by the caller's proven workspace, so a foreign id reports zeros. Requires `analytics:read`."
          },
          "response": []
        },
        {
          "name": "Breakdown by staff member",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/analytics/staff?from=2025-03-01&to=2025-03-01",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "analytics",
                "staff"
              ],
              "query": [
                {
                  "key": "from",
                  "value": "2025-03-01",
                  "disabled": false
                },
                {
                  "key": "to",
                  "value": "2025-03-01",
                  "disabled": false
                },
                {
                  "key": "locationId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "staffProfileId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                }
              ]
            },
            "description": "The window is a pair of inclusive calendar dates, not instants: \"1 March\" means 1 March where the business is, resolved against the workspace timezone inside PostgreSQL. Both `from` and `to` are required — an analytics view always has a visible period, and demanding one stops a bare request from aggregating a workspace's entire history. Neither `locationId` nor `staffProfileId` is a tenant selector: both narrow a query already filtered by the caller's proven workspace, so a foreign id reports zeros. Requires `analytics:read`."
          },
          "response": []
        },
        {
          "name": "Breakdown by service",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/analytics/services?from=2025-03-01&to=2025-03-01",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "analytics",
                "services"
              ],
              "query": [
                {
                  "key": "from",
                  "value": "2025-03-01",
                  "disabled": false
                },
                {
                  "key": "to",
                  "value": "2025-03-01",
                  "disabled": false
                },
                {
                  "key": "locationId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "staffProfileId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                }
              ]
            },
            "description": "The window is a pair of inclusive calendar dates, not instants: \"1 March\" means 1 March where the business is, resolved against the workspace timezone inside PostgreSQL. Both `from` and `to` are required — an analytics view always has a visible period, and demanding one stops a bare request from aggregating a workspace's entire history. Neither `locationId` nor `staffProfileId` is a tenant selector: both narrow a query already filtered by the caller's proven workspace, so a foreign id reports zeros. Requires `analytics:read`."
          },
          "response": []
        },
        {
          "name": "Breakdown by location",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/analytics/locations?from=2025-03-01&to=2025-03-01",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "analytics",
                "locations"
              ],
              "query": [
                {
                  "key": "from",
                  "value": "2025-03-01",
                  "disabled": false
                },
                {
                  "key": "to",
                  "value": "2025-03-01",
                  "disabled": false
                },
                {
                  "key": "locationId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "staffProfileId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                }
              ]
            },
            "description": "The window is a pair of inclusive calendar dates, not instants: \"1 March\" means 1 March where the business is, resolved against the workspace timezone inside PostgreSQL. Both `from` and `to` are required — an analytics view always has a visible period, and demanding one stops a bare request from aggregating a workspace's entire history. Neither `locationId` nor `staffProfileId` is a tenant selector: both narrow a query already filtered by the caller's proven workspace, so a foreign id reports zeros. Requires `analytics:read`."
          },
          "response": []
        },
        {
          "name": "Demand by day and hour",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/analytics/peak-times?from=2025-03-01&to=2025-03-01",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "analytics",
                "peak-times"
              ],
              "query": [
                {
                  "key": "from",
                  "value": "2025-03-01",
                  "disabled": false
                },
                {
                  "key": "to",
                  "value": "2025-03-01",
                  "disabled": false
                },
                {
                  "key": "locationId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "staffProfileId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                }
              ]
            },
            "description": "The window is a pair of inclusive calendar dates, not instants: \"1 March\" means 1 March where the business is, resolved against the workspace timezone inside PostgreSQL. Both `from` and `to` are required — an analytics view always has a visible period, and demanding one stops a bare request from aggregating a workspace's entire history. Neither `locationId` nor `staffProfileId` is a tenant selector: both narrow a query already filtered by the caller's proven workspace, so a foreign id reports zeros. Requires `analytics:read`."
          },
          "response": []
        },
        {
          "name": "Customer acquisition and retention",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/analytics/customers?from=2025-03-01&to=2025-03-01",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "analytics",
                "customers"
              ],
              "query": [
                {
                  "key": "from",
                  "value": "2025-03-01",
                  "disabled": false
                },
                {
                  "key": "to",
                  "value": "2025-03-01",
                  "disabled": false
                },
                {
                  "key": "locationId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "staffProfileId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                }
              ]
            },
            "description": "The window is a pair of inclusive calendar dates, not instants: \"1 March\" means 1 March where the business is, resolved against the workspace timezone inside PostgreSQL. Both `from` and `to` are required — an analytics view always has a visible period, and demanding one stops a bare request from aggregating a workspace's entire history. Neither `locationId` nor `staffProfileId` is a tenant selector: both narrow a query already filtered by the caller's proven workspace, so a foreign id reports zeros. Requires `analytics:read`."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Reports",
      "description": "The same rows listed rather than aggregated, plus CSV export.",
      "item": [
        {
          "name": "List appointments as a report",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/reports/appointments",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "reports",
                "appointments"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "50",
                  "disabled": true
                },
                {
                  "key": "from",
                  "value": "2025-03-01",
                  "disabled": true
                },
                {
                  "key": "to",
                  "value": "2025-03-01",
                  "disabled": true
                },
                {
                  "key": "status",
                  "value": "PENDING",
                  "disabled": true
                },
                {
                  "key": "serviceId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "staffProfileId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "locationId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                }
              ]
            },
            "description": "Requires `reports:read`. `from`/`to` are optional here, unlike on the analytics surface — a report is a record-keeping tool, and \"every appointment this branch has ever had\" is a legitimate question. When both are supplied the same 366-day cap applies, so a range filter cannot be used to force an unindexed scan."
          },
          "response": []
        },
        {
          "name": "Export the appointment report as CSV",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/reports/appointments.csv",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "reports",
                "appointments.csv"
              ],
              "query": [
                {
                  "key": "from",
                  "value": "2025-03-01",
                  "disabled": true
                },
                {
                  "key": "to",
                  "value": "2025-03-01",
                  "disabled": true
                },
                {
                  "key": "status",
                  "value": "PENDING",
                  "disabled": true
                },
                {
                  "key": "serviceId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "staffProfileId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "locationId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                }
              ]
            },
            "description": "Requires `reports:read` **and** `reports:export`: an export is a read that leaves the building, so it needs the permission to read and the separate permission to take a copy. Takes the same filters as the JSON report so the file always reproduces the table an operator was looking at, but no pagination — it is bounded by a hard row cap instead, because a page number would only produce a truncated file that looks complete. The response carries `X-Report-Row-Limit`, `X-Report-Matched-Rows` and `X-Report-Truncated` headers, and a Content-Disposition that makes a browser save it."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Audit",
      "description": "The workspace's own copy of the trail every mutating service writes to. Read-only, and that is the property worth having: nothing on this surface can amend or delete an entry, so the record of what happened cannot be edited by whoever it happened to. Scoped by the query itself rather than by a filter applied afterwards — another tenant's entries and platform-level entries cannot be named, only missed. Entries carry the actor as a snapshot taken at the time, so a person renaming themselves later does not rewrite history.",
      "item": [
        {
          "name": "Read the workspace audit trail",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/audit-logs",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "audit-logs"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "20",
                  "disabled": true
                },
                {
                  "key": "action",
                  "value": "",
                  "disabled": true
                },
                {
                  "key": "entityType",
                  "value": "",
                  "disabled": true
                },
                {
                  "key": "entityId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "actorUserId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "from",
                  "value": "2025-03-01",
                  "disabled": true
                },
                {
                  "key": "to",
                  "value": "2025-03-01",
                  "disabled": true
                },
                {
                  "key": "search",
                  "value": "",
                  "disabled": true
                }
              ]
            },
            "description": "Requires `audit:read`. Newest first, filterable by `action`, `entityType`, `entityId`, `actorUserId`, a date range and a free-text `search` over the actor, the verb and the entity type. `from` and `to` are calendar dates cut into whole days on the *workspace* clock rather than in UTC — a tenant asking what happened on 3 March means their 3 March — and the zone used is reported back as `meta.timezone` so a client can label its date pickers honestly."
          },
          "response": []
        },
        {
          "name": "Export the audit trail as CSV",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/audit-logs/export.csv",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "audit-logs",
                "export.csv"
              ],
              "query": [
                {
                  "key": "action",
                  "value": "",
                  "disabled": true
                },
                {
                  "key": "entityType",
                  "value": "",
                  "disabled": true
                },
                {
                  "key": "entityId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "actorUserId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "from",
                  "value": "2025-03-01",
                  "disabled": true
                },
                {
                  "key": "to",
                  "value": "2025-03-01",
                  "disabled": true
                },
                {
                  "key": "search",
                  "value": "",
                  "disabled": true
                }
              ]
            },
            "description": "Requires `audit:read` **and** `reports:export`, on the same reading as the appointment export: taking a copy out of the product is a separate act from reading it on screen. Takes the same filters as the list and no pagination — it is bounded by a hard row cap instead, because a page number would only produce a truncated file that looks complete. The response carries `X-Report-Row-Limit`, `X-Report-Matched-Rows` and `X-Report-Truncated`, plus `Cache-Control: no-store`, since the file names people, addresses and what they did. The export writes an audit entry of its own before the first byte: an owner taking the trail out of the product is exactly the event the trail exists to record."
          },
          "response": []
        },
        {
          "name": "Read one audit entry",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/audit-logs/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "audit-logs",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires `audit:read`. An entry belonging to another workspace, or a platform-level entry with no workspace at all, answers 404 rather than 403 — the id is matched against the caller's own workspace in the same statement, so a foreign id simply finds no row. Adds `userAgent` to the list shape: it is repetitive twenty rows at a time and exactly what an investigation into one entry wants."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Platform Admin",
      "description": "Operating the platform itself, and the only endpoints that read across tenants. They require `platformRole = ADMIN` and are not tenant-scoped — X-Business-Id plays no part here, because an operator holds no membership in the workspaces they administer. What they expose is workspaces, platform accounts and counts; never customer names, contact details, appointment contents or notes. Running the platform is no reason to read a clinic's patient list, and the shape of these responses is what enforces that.",
      "item": [
        {
          "name": "Platform-wide headline figures",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/admin/overview",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "admin",
                "overview"
              ]
            },
            "description": "Requires platform administrator access. Counts workspaces, accounts, appointments and customers across every tenant. Volume is cut on when a booking was taken rather than on when it falls due, so the fourteen-day series answers \"how much work came in\" rather than \"how full is the diary\"; only the upcoming counter reads the appointment start. The customer figure is a total and nothing else — no part of this payload identifies a person a workspace books."
          },
          "response": []
        },
        {
          "name": "List every workspace on the platform",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/admin/workspaces",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "admin",
                "workspaces"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "20",
                  "disabled": true
                },
                {
                  "key": "search",
                  "value": "",
                  "disabled": true
                },
                {
                  "key": "status",
                  "value": "ACTIVE",
                  "disabled": true
                },
                {
                  "key": "sort",
                  "value": "newest",
                  "disabled": true
                }
              ]
            },
            "description": "Requires platform administrator access. The one directory in the product that spans tenants, which is why each row carries counts and the owning account rather than any of the workspace's own records. `search` matches name or slug case-insensitively with LIKE wildcards escaped, so a search for `%` looks for a literal per cent sign instead of matching everything."
          },
          "response": []
        },
        {
          "name": "Read one workspace",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/admin/workspaces/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "admin",
                "workspaces",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires platform administrator access. Adds the profile fields, the member list, a breakdown of appointments by status and the twenty most recent audit entries for this workspace. Members are platform accounts, not customers: an operator can see who administers a clinic and never who it treats. A soft-deleted workspace answers 404 like any unknown id — it is no longer administrable, and must be indistinguishable from one that never existed."
          },
          "response": []
        },
        {
          "name": "Suspend, archive or restore a workspace",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/admin/workspaces/:id/status",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "admin",
                "workspaces",
                ":id",
                "status"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires platform administrator access. Suspending a workspace closes it from both directions at once: tenant resolution only accepts an ACTIVE business, so every management call its members make begins answering 404, and its public booking pages stop serving. Nothing is deleted and no appointment is cancelled — setting the workspace back to ACTIVE returns it exactly as it was. The optional `reason` is stored only on the audit row, written in the same transaction as the change, because \"why was this suspended\" is a question that arrives weeks later.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"status\": \"ACTIVE\",\n  \"reason\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "List platform accounts",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/admin/users",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "admin",
                "users"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "20",
                  "disabled": true
                },
                {
                  "key": "search",
                  "value": "",
                  "disabled": true
                },
                {
                  "key": "status",
                  "value": "ACTIVE",
                  "disabled": true
                },
                {
                  "key": "platformRole",
                  "value": "ADMIN",
                  "disabled": true
                },
                {
                  "key": "sort",
                  "value": "newest",
                  "disabled": true
                }
              ]
            },
            "description": "Requires platform administrator access. These are the people who sign in, not the customers a workspace books — the two never meet on this surface. `search` matches email, first name or last name case-insensitively. `workspaceCount` ignores memberships in soft-deleted workspaces, so it always equals the length of the membership list the detail endpoint returns."
          },
          "response": []
        },
        {
          "name": "Read one platform account",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/admin/users/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "admin",
                "users",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires platform administrator access. Adds contact and locale settings, lockout state, the number of refresh-token sessions still live, and every workspace the account belongs to with the role it holds there. `activeSessionCount` is the honest answer to \"is this person still signed in somewhere\", which is what an operator needs both before suspending an account and immediately afterwards."
          },
          "response": []
        },
        {
          "name": "Suspend, deactivate or reinstate an account",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/admin/users/:id/status",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "admin",
                "users",
                ":id",
                "status"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires platform administrator access. Suspending or deactivating an account revokes every live refresh token in the same transaction as the status change, so a session cannot outlive the decision; authentication refuses a SUSPENDED or DEACTIVATED user on every request, so the access token already in their hands dies at its next call too. Reinstating revokes nothing — the person simply signs in again. Changing your own status is refused with 409: an operator who suspends themselves has closed the only surface that could let them back in. INVITED is not accepted, because it is a state the invitation flow owns and setting it by hand would strand the account.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"status\": \"ACTIVE\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Grant or withdraw platform administrator access",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/admin/users/:id/platform-role",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "admin",
                "users",
                ":id",
                "platform-role"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "00000000-0000-0000-0000-000000000000"
                }
              ]
            },
            "description": "Requires platform administrator access. ADMIN is not a workspace role: it grants this entire surface and nothing at all inside any tenant. Two demotions are refused with 409 — your own, and one that would leave the platform with no ACTIVE administrator. The second is decided under a row lock, so two operators demoting each other at the same moment cannot both read \"there is still another admin\" and both succeed.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"platformRole\": \"ADMIN\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Search the audit trail across tenants",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/admin/audit-logs",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "admin",
                "audit-logs"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "20",
                  "disabled": true
                },
                {
                  "key": "businessId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "action",
                  "value": "",
                  "disabled": true
                },
                {
                  "key": "actorUserId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "entityType",
                  "value": "",
                  "disabled": true
                },
                {
                  "key": "from",
                  "value": "2025-03-01",
                  "disabled": true
                },
                {
                  "key": "to",
                  "value": "2025-03-01",
                  "disabled": true
                }
              ]
            },
            "description": "Requires platform administrator access. The same rows a workspace sees in its own activity feed, plus the platform-level ones that belong to no tenant and carry a null `businessId`. `from` and `to` are inclusive calendar dates compared against the moment the row was written, and rows come back newest first. `businessId` narrows the search here rather than asserting a tenant — the distinction that makes this surface possible at all."
          },
          "response": []
        },
        {
          "name": "Dependency and delivery-backlog status",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/admin/health",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "admin",
                "health"
              ]
            },
            "description": "Requires platform administrator access. Distinct from the unauthenticated /health and /ready probes, which answer \"is this process alive\" for an orchestrator: this one reports on PostgreSQL, Redis and the notification outbox, which is operator information rather than something to expose at the edge. It answers 200 even when a dependency is down — a 503 would make the one page that could explain an outage disappear during one — so read `database.ok`, `redis.ok` and the outbox counters rather than the status code."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Customer Portal",
      "description": "A customer reading and changing their own bookings, across every workspace that has a record of them. **Authenticated but not tenant-scoped** — X-Business-Id plays no part here, because a customer holds no membership to select among; they are a person who appears in one or more workspaces' address books. Every response is built from the customer records tied to the signed-in account, so this surface returns only rows belonging to the person asking, and a booking reference belonging to somebody else is indistinguishable from one that never existed: both answer 404. Cancelling or rescheduling runs the workspace booking policy exactly as the anonymous manage link does, so signing in is never a way round a notice period — including for somebody who happens to be a member of that workspace as well.",
      "item": [
        {
          "name": "Read my profile and the workspaces that know me",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/me/profile",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "me",
                "profile"
              ]
            },
            "description": "Authenticated, not tenant-scoped, and no permission is checked — there is no membership to carry one. Reading this also reconciles the account with any address-book record that shares its email and is not yet linked, but **only once the address has been verified**: registration accepts any address, so linking on a bare email match would be an account-takeover primitive. Past that gate the link grants nothing new, since every one of those bookings already had its manage link delivered to that mailbox."
          },
          "response": []
        },
        {
          "name": "List my bookings",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/me/bookings",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "me",
                "bookings"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1",
                  "disabled": true
                },
                {
                  "key": "pageSize",
                  "value": "20",
                  "disabled": true
                },
                {
                  "key": "when",
                  "value": "ALL",
                  "disabled": true
                },
                {
                  "key": "status",
                  "value": "PENDING",
                  "disabled": true
                }
              ]
            },
            "description": "Every workspace at once, which is the point: a person does not think of their diary one business at a time. `when` names the intent — UPCOMING sorts soonest first, PAST sorts most recent first — so a dashboard does not have to send a sort order it would get wrong half the time. Rows are the summary shape: enough to render a card and decide what to open, with `internalNotes` and the rest of the staff-facing detail deliberately absent."
          },
          "response": []
        },
        {
          "name": "Read one of my bookings",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/me/bookings/:publicId",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "me",
                "bookings",
                ":publicId"
              ],
              "variable": [
                {
                  "key": "publicId",
                  "value": "apt_00000000000000000000000000"
                }
              ]
            },
            "description": "The same projection the anonymous manage link serves, reused rather than reimplemented so the two surfaces cannot drift on what a customer may see. Ownership is part of the query rather than a check applied to its result: a handle belonging to somebody else and a handle that never existed are the same 404."
          },
          "response": []
        },
        {
          "name": "Cancel one of my bookings",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/me/bookings/:publicId/cancel",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "me",
                "bookings",
                ":publicId",
                "cancel"
              ],
              "variable": [
                {
                  "key": "publicId",
                  "value": "apt_00000000000000000000000000"
                }
              ]
            },
            "description": "Runs the workspace's own cancellation policy — the same code path, and the same notice deadline, as the anonymous manage link. Being signed in makes the cancellation attributable, not free: a 422 inside the deadline is the business rule answering, and it answers for a caller who happens to be a member of that workspace too, so this surface cannot become a policy bypass. The status history records a CUSTOMER actor and the audit entry names the user account, which is the one thing an anonymous manage link can never record.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"reason\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Move one of my bookings",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/me/bookings/:publicId/reschedule",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "me",
                "bookings",
                ":publicId",
                "reschedule"
              ],
              "variable": [
                {
                  "key": "publicId",
                  "value": "apt_00000000000000000000000000"
                }
              ]
            },
            "description": "Only the new instant is accepted. Provider and location are deliberately refused, as they are on the anonymous surface: the appointment already names both, and letting the customer reassign them would turn a reschedule into a way to book any provider in the workspace, outside whatever the booking link publishes. The reschedule deadline, the notice period and the availability check all apply, and a slot lost to a race answers 409.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"startsAt\": \"2025-03-01T09:00:00Z\",\n  \"reason\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Read my contact preferences",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/me/preferences",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "me",
                "preferences"
              ]
            },
            "description": "Folded across every workspace that holds a record of this person, because there is no workspace to pick between — naming one would mean accepting the identifier this surface must never take. When the linked records disagree, which they can because staff edit the same column from the address book, the reported value is the conservative reading and `divergent` is true, so a client can say \"varies\" rather than presenting one workspace's answer as though it were universal."
          },
          "response": []
        },
        {
          "name": "Change my contact preferences",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/me/preferences",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "me",
                "preferences"
              ]
            },
            "description": "Applied to every linked record in one transaction, with an audit entry written per workspace so each business can see the change in its own trail. Merged over what is stored rather than replacing it, and no field has a default — a default would quietly reset the switches the person left alone. `reminderOffsetsMinutes: null` hands the schedule back to each workspace's own reminder policy. A patch from somebody with no linked records answers 409 rather than a 200 that stored nothing.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"emailEnabled\": false,\n  \"smsEnabled\": false,\n  \"marketingOptIn\": false,\n  \"reminderOffsetsMinutes\": [\n    0\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        }
      ]
    },
    {
      "name": "Public Booking",
      "description": "The unauthenticated booking surface. Tenant context comes from the booking-link slug in the path; existing bookings are addressed only by their opaque apt_ handle.",
      "item": [
        {
          "name": "Read a public booking page",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/public/booking-links/:slug",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "public",
                "booking-links",
                ":slug"
              ],
              "variable": [
                {
                  "key": "slug",
                  "value": "example"
                }
              ]
            },
            "description": "The workspace is derived from the slug — no tenant identifier is accepted anywhere on this surface. Returns only what the link publishes: its branding, its custom questions and the offering it actually exposes.",
            "auth": {
              "type": "noauth"
            }
          },
          "response": []
        },
        {
          "name": "Search bookable slots on a public booking page",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/public/booking-links/:slug/availability?serviceId=00000000-0000-0000-0000-000000000000&fromDate=2025-03-01&toDate=2025-03-01&timezone=Asia/Kolkata",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "public",
                "booking-links",
                ":slug",
                "availability"
              ],
              "query": [
                {
                  "key": "serviceId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": false
                },
                {
                  "key": "staffProfileId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "locationId",
                  "value": "00000000-0000-0000-0000-000000000000",
                  "disabled": true
                },
                {
                  "key": "fromDate",
                  "value": "2025-03-01",
                  "disabled": false
                },
                {
                  "key": "toDate",
                  "value": "2025-03-01",
                  "disabled": false
                },
                {
                  "key": "timezone",
                  "value": "Asia/Kolkata",
                  "disabled": false
                }
              ],
              "variable": [
                {
                  "key": "slug",
                  "value": "example"
                }
              ]
            },
            "description": "Rate limited more tightly than the rest of the surface: a wide date range fans out into a slot search per provider. `timezone` is the customer's own zone and bounds the search to their calendar days, so \"next Tuesday\" means their Tuesday and not the workspace's. `staffProfileId` is honoured only when the link lets customers choose their provider.",
            "auth": {
              "type": "noauth"
            }
          },
          "response": []
        },
        {
          "name": "Book through a public booking page",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Idempotency-Key",
                "value": "booking-2f7c1e90-1a2b-4c3d-9e8f-0a1b2c3d4e5f",
                "type": "text",
                "description": "A client-chosen key that makes a booking request safe to retry. A repeat carrying the same key answers with the original booking instead of creating a second one — the management route reports this as `meta.replayed`, the public route by answering 200 rather than 201.",
                "disabled": true
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/public/booking-links/:slug/bookings",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "public",
                "booking-links",
                ":slug",
                "bookings"
              ],
              "variable": [
                {
                  "key": "slug",
                  "value": "example"
                }
              ]
            },
            "description": "No customer identifier is accepted: a caller who could name an existing customer row could attach their booking — and the confirmation email — to somebody else's record, so the service matches on the submitted email inside the tenant instead. Omitting `staffProfileId` lets Smart Match assign a provider. `answers` is checked against the questions this link publishes and anything it did not ask for is dropped. Send X-Idempotency-Key to make a retry safe.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"serviceId\": \"00000000-0000-0000-0000-000000000000\",\n  \"staffProfileId\": \"00000000-0000-0000-0000-000000000000\",\n  \"locationId\": \"00000000-0000-0000-0000-000000000000\",\n  \"startsAt\": \"2025-03-01T09:00:00Z\",\n  \"timezone\": \"Asia/Kolkata\",\n  \"customer\": {\n    \"firstName\": \"Ada\",\n    \"lastName\": \"Lovelace\",\n    \"email\": \"customer@example.com\",\n    \"phone\": \"+91 98765 43210\"\n  },\n  \"customerNotes\": \"\",\n  \"answers\": {}\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "auth": {
              "type": "noauth"
            }
          },
          "response": []
        },
        {
          "name": "Read one booking by its public reference",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/public/appointments/:publicId",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "public",
                "appointments",
                ":publicId"
              ],
              "variable": [
                {
                  "key": "publicId",
                  "value": "apt_00000000000000000000000000"
                }
              ]
            },
            "description": "Addressed by the opaque `apt_…` handle, never by an internal UUID. That handle behaves like a bearer token, which is why this route never widens: it reads exactly the one appointment named in the path.",
            "auth": {
              "type": "noauth"
            }
          },
          "response": []
        },
        {
          "name": "Move a booking from its manage link",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/public/appointments/:publicId/reschedule",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "public",
                "appointments",
                ":publicId",
                "reschedule"
              ],
              "variable": [
                {
                  "key": "publicId",
                  "value": "apt_00000000000000000000000000"
                }
              ]
            },
            "description": "The customer moves the time and nothing else. Provider and location are deliberately not accepted: the appointment already names them, and letting an unauthenticated caller reassign it would turn the manage link into a way to book any provider in the workspace, outside the offering its booking link publishes. Subject to the workspace's reschedule deadline and its per-appointment limit.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"startsAt\": \"2025-03-01T09:00:00Z\",\n  \"reason\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "auth": {
              "type": "noauth"
            }
          },
          "response": []
        },
        {
          "name": "Cancel a booking from its manage link",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/public/appointments/:publicId/cancel",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "public",
                "appointments",
                ":publicId",
                "cancel"
              ],
              "variable": [
                {
                  "key": "publicId",
                  "value": "apt_00000000000000000000000000"
                }
              ]
            },
            "description": "Subject to the workspace's cancellation deadline and to `allowCustomerCancel`. The optional reason reaches the status history and the operator's notification.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"reason\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "auth": {
              "type": "noauth"
            }
          },
          "response": []
        }
      ]
    },
    {
      "name": "Notification Templates",
      "item": [
        {
          "name": "List the messages this workspace sends",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/notification-templates",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "notification-templates"
              ],
              "query": [
                {
                  "key": "channel",
                  "value": "EMAIL",
                  "disabled": true
                }
              ]
            },
            "description": "Every message MeetFlow defines, with the copy going out today and whether it is the workspace's own or the built-in default. Driven from the catalogue rather than from the overrides table, so a workspace that has never edited anything still sees what is being sent in its name. Not paginated: there is one page and there always will be. Bodies interpolate `{{placeholder}}` names, and an unknown one renders as **empty text** rather than failing — so a typo would ship an email opening \"Hi ,\" to every customer with nothing logged. Every write is therefore checked against the catalogue that message can fill, returned on the read as `placeholders`, and a body naming anything else is refused with 422 listing both the offender and the alternatives. Requires `templates:manage`, which the built-in roles grant to owners and managers — rewriting a confirmation email is operational work rather than an act of workspace authority."
          },
          "response": []
        },
        {
          "name": "Read one message",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/notification-templates/:key/:channel",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "notification-templates",
                ":key",
                ":channel"
              ],
              "variable": [
                {
                  "key": "key",
                  "value": "BOOKING_CONFIRMATION"
                },
                {
                  "key": "channel",
                  "value": "EMAIL"
                }
              ]
            },
            "description": "The same shape as one row of the listing. `defaultSubject` and `defaultBodyText` are always present, whether or not an override exists, so the editor can diff against the default and offer to restore it without a second request. Bodies interpolate `{{placeholder}}` names, and an unknown one renders as **empty text** rather than failing — so a typo would ship an email opening \"Hi ,\" to every customer with nothing logged. Every write is therefore checked against the catalogue that message can fill, returned on the read as `placeholders`, and a body naming anything else is refused with 422 listing both the offender and the alternatives. Requires `templates:manage`, which the built-in roles grant to owners and managers — rewriting a confirmation email is operational work rather than an act of workspace authority."
          },
          "response": []
        },
        {
          "name": "Replace one message with the workspace’s own copy",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/notification-templates/:key/:channel",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "notification-templates",
                ":key",
                ":channel"
              ],
              "variable": [
                {
                  "key": "key",
                  "value": "BOOKING_CONFIRMATION"
                },
                {
                  "key": "channel",
                  "value": "EMAIL"
                }
              ]
            },
            "description": "Creates the override or replaces it; the address is fixed by the key and channel, so this never creates a resource at a URL the caller did not already have and answers 200 rather than 201. An email without a subject is refused — every spam filter reads a blank one exactly as it looks. There is no `bodyHtml` field and there will not be: the HTML part is generated from the text with every interpolated value escaped, and accepting tenant-authored markup would put it inside a message MeetFlow's own domain is signing. `isActive: false` parks a draft — the row is kept and the default is what customers receive. Bodies interpolate `{{placeholder}}` names, and an unknown one renders as **empty text** rather than failing — so a typo would ship an email opening \"Hi ,\" to every customer with nothing logged. Every write is therefore checked against the catalogue that message can fill, returned on the read as `placeholders`, and a body naming anything else is refused with 422 listing both the offender and the alternatives. Requires `templates:manage`, which the built-in roles grant to owners and managers — rewriting a confirmation email is operational work rather than an act of workspace authority.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"subject\": \"\",\n  \"bodyText\": \"\",\n  \"isActive\": false\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Restore MeetFlow’s copy",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/notification-templates/:key/:channel",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "notification-templates",
                ":key",
                ":channel"
              ],
              "variable": [
                {
                  "key": "key",
                  "value": "BOOKING_CONFIRMATION"
                },
                {
                  "key": "channel",
                  "value": "EMAIL"
                }
              ]
            },
            "description": "Drops the override entirely rather than deactivating it or replacing it with a snapshot, so the next send reads the built-in default and picks up every later improvement to it — which is why defaults are never copied into a workspace in the first place. Answers 404 when there is no override: telling an operator their edit was undone when there was no edit is worse than saying there is nothing here. Requires `templates:manage`."
          },
          "response": []
        },
        {
          "name": "Render a draft against sample data",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Business-Id",
                "value": "{{businessId}}",
                "type": "text",
                "description": "Selects which of the caller's own workspaces the request acts in. It is never a tenant assertion: the value is only used to pick among ACTIVE memberships the authenticated user already holds, and an id they do not belong to answers 404. Optional when the user belongs to exactly one workspace; required — with a 403 asking them to choose — when they belong to several."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/notification-templates/:key/:channel/preview",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "notification-templates",
                ":key",
                ":channel",
                "preview"
              ],
              "variable": [
                {
                  "key": "key",
                  "value": "BOOKING_CONFIRMATION"
                },
                {
                  "key": "channel",
                  "value": "EMAIL"
                }
              ]
            },
            "description": "Takes the draft in the request rather than reading the saved row, because the question being asked is \"should I save this\" and answering it after the save is answering it too late. Reads and writes nothing, but is a POST because a body somebody is still editing does not belong in a query string, a proxy log, or browser history. The same validation runs, so a preview can never show a draft the save would refuse. Requires `templates:manage`.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"subject\": \"\",\n  \"bodyText\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        }
      ]
    }
  ]
}
